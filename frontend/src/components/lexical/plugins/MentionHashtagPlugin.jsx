export function MentionHashtagPlugin() {
  // This plugin is now only used for maintaining node definitions
  // Transformation only happens via AutocompletePlugin when user selects from list
  return null;
}
